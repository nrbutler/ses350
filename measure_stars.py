import numpy as np
import scipy as sp

def measure_stars(image,x,y):
    #
    # you wrote code previously to return the fluxes at positions x,y in an image
    #
    # replace the following with your code:
    flux = image[x,y]
    #
    return flux
